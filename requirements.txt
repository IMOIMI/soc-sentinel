# SOC-Sentinel keeps dependencies intentional and lean.
# Standard-library modules handle .eml parsing, JSON log analysis,
# hashing, timelines, and heuristic correlation wherever possible.

# Flask powers the lightweight SIEM dashboard used for screenshots and demos.
Flask==3.1.2

# ReportLab generates PDF incident response reports from finalized alerts.
reportlab==4.4.3
