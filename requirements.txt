# SOC-Sentinel keeps dependencies intentional and lean.
# Standard-library modules handle .eml parsing, JSON log analysis,
# hashing, timelines, and heuristic correlation wherever possible.

# FastAPI powers the API layer for scans, log ingestion, incidents, and response endpoints.
fastapi==0.116.1

# Uvicorn runs the FastAPI app locally and in lightweight deployments.
uvicorn[standard]==0.35.0

# Flask powers the lightweight SIEM dashboard used for screenshots and demos.
Flask==3.1.2

# Pydantic defines validated alert, incident, scan, and API response schemas.
pydantic==2.11.7

# Beautiful Soup parses HTML email bodies so the phishing engine can inspect visible text and links.
beautifulsoup4==4.13.4

# tldextract normalizes domains and separates subdomain/domain/suffix for link reputation heuristics.
tldextract==5.3.0

# python-dotenv loads local API keys and environment settings without hardcoding secrets.
python-dotenv==1.1.1

# ReportLab generates PDF incident response reports from finalized alerts.
reportlab==4.4.3
